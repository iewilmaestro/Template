
### FaucetAtbRv2
```php
data = [
  "antibotlinks"          => $atb,
  "csrf_token_name"       => $csrf,
  "token"                 => $token,
  "captcha"               => "recaptchav2",
  "g-recaptcha-response"  => $cap,
  "wallet"                => urlencode(simpan("Email_Faucetpay"))
];
host."faucet/verify/".$coin
```
